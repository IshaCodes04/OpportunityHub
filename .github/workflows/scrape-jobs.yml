name: 🚀 Scrape & Update Jobs

on:
  schedule:
    # Run daily at 01:30 UTC (7:00 AM IST)
    - cron: '30 1 * * *'
  # Manual trigger
  workflow_dispatch:

jobs:
  scrape-jobs:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🐍 Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: 🌐 Set up Chrome
        id: setup-chrome
        uses: browser-actions/setup-chrome@v2
        with:
          chrome-version: stable
          install-dependencies: true

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 🔍 Run job scraper
        env:
          CHROME_BIN: ${{ steps.setup-chrome.outputs.chrome-path }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          GMAIL_SENDER: ishu010.com@gmail.com
          GMAIL_RECIPIENT: ishu010.com@gmail.com
        run: |
          python main.py

      - name: 📝 Commit and push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add -A
          TIMESTAMP=$(date -u +"%Y-%m-%d %H:%M UTC")
          
          if git diff --cached --quiet; then
            echo "ℹ️  No changes to commit"
          else
            git commit -m "🔄 Auto Update: Job opportunities — ${TIMESTAMP}"
            git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git HEAD:main
            echo "✅ Changes pushed to GitHub!"
          fi

      - name: ✅ Scrape completed successfully
        run: echo "🎉 Jobs updated and pushed to GitHub!"
